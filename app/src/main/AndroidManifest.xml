<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aser.blinds">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OtherPlaces"
            android:label="Other Places"></activity>
        <activity android:name=".setLocationMap"
            android:label="Set Location"/>
        <activity android:name=".SetLocation"
            android:label="Set LOcation"/>
        <activity android:name=".Office"
            android:label="Navigation"/>
        <activity android:name=".Emergancy_Number"
            android:label="Emergency Number"/>
        <activity android:name=".MainActivity"
            android:label="Blind Mode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Guardian"
            android:label="Guardian Mode" />
        <activity
            android:name=".FavriouteLocation"
            android:label="Favroiute Locations" />
    </application>

</manifest>